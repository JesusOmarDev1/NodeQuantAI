COLUMNAS REGRESIÓN — evaluar_modelos.py
========================================

# Fuente: db/ganglios_master.csv (57 features radiómicas + Paciente_ID + target_riesgo)

# ── Targets (creados desde shape_* antes de eliminarlas) ──
target_regresion  = shape_VoxelVolume        # Volumen tumoral (mm³)
target_eje_corto  = shape_MinorAxisLength    # Diámetro eje corto (mm)
target_eje_largo  = shape_MajorAxisLength    # Diámetro eje largo (mm)

# ── Prevención de Data Leakage ──
# Se eliminan TODAS las columnas shape_* (14 features) para que
# el modelo infiera dimensiones geométricas exclusivamente desde
# características de textura e intensidad radiológica.

# ── Features finales: 42 (firstorder=19 + glcm=24) ──
# Se eliminan: Paciente_ID, target_riesgo, target_*, shape_*
X = df.drop(columns=["Paciente_ID", "target_riesgo"] + COLS_TARGET)
# Resultado: solo columnas firstorder_* y glcm_*